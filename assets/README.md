# Assets Directory

Place the `doc-drift-caught.gif` screenshot/recording here.

To capture it:
1. Push this repo to GitHub
2. Open a PR that modifies a `.py` file without updating `docs/`
3. Wait for the GitHub Action to fail and post a comment
4. Screen-record the PR page showing the failed check and bot comment
5. Save as `doc-drift-caught.gif` in this directory
